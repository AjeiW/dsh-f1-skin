# raw downloads & exploration scratch — the compressed *.jpg are the
# embedded assets; the -raw originals stay local for re-compression only.
assets/cockpits/*-raw.*
assets/cockpit-candidates.jsonl

# Local development and browser-test output.
node_modules/
.npm-cache/
.tmp/
test-results/
playwright-report/
blob-report/
