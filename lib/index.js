// dsh-f1-skin — node half. The whole skin lives in the client bundle
// ("./client" export); this host-side plugin exists so the loader entry
// has a fiber and the dsh.client row joins the browser module graph.
export function apply() {}
